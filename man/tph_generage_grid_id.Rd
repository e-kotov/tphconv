% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tph_generage_grid_id.R
\name{tph_generage_grid_id}
\alias{tph_generage_grid_id}
\title{Generate INSPIRE / GISCO Grid-Cell IDs}
\usage{
tph_generage_grid_id(
  x,
  cellsize = NULL,
  crs = NULL,
  style = c("gisco_full", "gisco_short"),
  origin = c(0, 0),
  raster_mask_na = FALSE,
  raster_layer = 1
)
}
\arguments{
\item{x}{A \code{SpatVector}, \code{SpatRaster}, or \code{sf}/\code{sfc} object.}

\item{cellsize}{The grid resolution in the units of the object's CRS (e.g., meters).
If \code{NULL}, it will be inferred from the raster's resolution or the bounding
box of the first vector feature (if square).}

\item{crs}{An optional CRS string (e.g., "EPSG:3035") to use if the
object's CRS cannot be automatically determined.}

\item{style}{The format of the output ID string: "gisco_full" or "gisco_short".}

\item{origin}{A numeric vector of \code{c(x, y)} coordinates for the grid's origin.
Defaults to \code{c(0, 0)}.}

\item{raster_mask_na}{For \code{SpatRaster} input, if \code{TRUE}, IDs are only computed
for cells that are not \code{NA}.}

\item{raster_layer}{For \code{SpatRaster} input, the layer index to use for the
\code{NA} mask.}
}
\value{
A character vector of grid-cell IDs. For \code{SpatRaster} inputs, the
vector is named with the corresponding cell indices.
}
\description{
Computes standardized grid-cell identifiers for SpatVector, SpatRaster,
or sf/sfc objects based on the lower-left (south-west) corner coordinates.
}
\keyword{internal}
